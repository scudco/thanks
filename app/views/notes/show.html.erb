<div id="note_controls">
  <% form_tag note_path(@note), :method => 'delete' do %>
    <%= submit_tag 'Delete Note' %>
  <% end %>
  <%= link_to "Edit Note", edit_note_path(@note) %>
</div>
<%= render 'note', :note => @note %>
